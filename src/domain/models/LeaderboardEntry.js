export class LeaderboardEntry {
  constructor ({ rank, name, score, streak, avatar }) {
    this.rank = rank
    this.name = name
    this.score = score
    this.streak = streak
    this.avatar = avatar
  }
}
